The code is in PHP and as such, must be ran on a suitable platform. In its current state, it
requires the use of offline input files that are located within the same directory.
The information is extracted from the .htm input files and output into .txt files with the same name.
The program displays how many output files were made.